import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

export default function RecWorkout() {
	return (
		<SafeAreaView
			style={{ backgroundColor: "#242424", flex: 1 }}
		></SafeAreaView>
	);
}
